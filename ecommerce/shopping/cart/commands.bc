<# domain ECommerce\Shopping\Cart\Command;

use Core;
use Core\ValueObject\Identifier;
use ECommerce\Shopping\Entity;

create Create { <Entity\Cart> $cart };
create Checkout { <Identifier> $cart_id };

create AddProduct { <Identifier> $cart_id, <Entity\Product> $product };
create ChangeProductQuantity { <Identifier> $cart_id, <Entity\Product> $product };
create RemoveProduct { <Identifier> $cart_id, <Identifier> $product_id };

#>
